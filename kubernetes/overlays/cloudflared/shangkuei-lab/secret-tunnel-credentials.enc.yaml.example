# Cloudflare Tunnel credentials secret
# Get values from: terraform output (in cloudflared-shangkuei-lab)
#
# To create encrypted secret:
# 1. Copy this file: cp secret-tunnel-credentials.enc.yaml.example secret-tunnel-credentials.enc.yaml
# 2. Fill in the values from terraform output
# 3. Encrypt: make encrypt
#
# credentials.json format (required by cloudflare-tunnel Helm chart):
# - AccountTag: Cloudflare account ID
# - TunnelID: UUID of the tunnel (from terraform output tunnel_id)
# - TunnelSecret: Tunnel secret (from terraform output tunnel_secret)
---
apiVersion: v1
kind: Secret
metadata:
  name: cloudflared
  namespace: cloudflared
type: Opaque
stringData:
  credentials.json: |
    {
      "AccountTag": "YOUR_CLOUDFLARE_ACCOUNT_ID",
      "TunnelID": "YOUR_TUNNEL_ID",
      "TunnelSecret": "YOUR_TUNNEL_SECRET"
    }
