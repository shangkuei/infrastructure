---
# Flux Kustomization for cert-manager
# Automated certificate management with Gateway API support
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cert-manager
  namespace: flux-system
spec:
  interval: 30m
  timeout: 10m
  prune: true
  wait: true

  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system

  path: ./kubernetes/overlays/cert-manager/shangkuei-lab

  # Dependencies:
  # - gateway-api-standard: Gateway API CRDs for HTTP01/TLS challenges
  # - cilium: CNI must be ready for network connectivity
  dependsOn:
    - name: gateway-api-standard
    - name: cilium

  healthChecks:
    - apiVersion: apps/v1
      kind: Deployment
      name: cert-manager
      namespace: cert-manager
    - apiVersion: apps/v1
      kind: Deployment
      name: cert-manager-webhook
      namespace: cert-manager
    - apiVersion: apps/v1
      kind: Deployment
      name: cert-manager-cainjector
      namespace: cert-manager
