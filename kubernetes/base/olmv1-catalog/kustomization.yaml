---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# OLM v1 ClusterCatalog Resources
# Deploys AFTER olmv1 operator-controller to ensure CRDs exist
# Provides access to OperatorHub community operators

resources:
  - clustercatalog-operatorhub.yaml
