---
# ArgoCD Application for Storage Example
# Demonstrates OpenEBS storage classes: Hostpath, ZFS, Mayastor
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: storage-example
  namespace: argocd
  labels:
    app.kubernetes.io/name: storage-example
    app.kubernetes.io/managed-by: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: argocd-examples

  source:
    repoURL: https://github.com/shangkuei/infrastructure.git
    targetRevision: HEAD
    path: argocd/storage-example/base

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd-example-storage

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
