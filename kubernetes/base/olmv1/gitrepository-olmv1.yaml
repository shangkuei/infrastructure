---
# OLM (Operator Lifecycle Manager) v1 from upstream repository
# Version: v1.5.1 (PINNED - operator-controller release)
# Provides: Operator lifecycle management, ClusterExtension, Extension CRDs
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: olmv1
  namespace: flux-system  # Source resources must be in flux-system
spec:
  url: https://github.com/operator-framework/operator-controller
  ref:
    tag: v1.5.1  # PINNED: Do not use 'branch' or 'semver' for production
  interval: 24h
  timeout: 60s
