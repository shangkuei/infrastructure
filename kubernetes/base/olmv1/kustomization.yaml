---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# OLM v1 (Operator Lifecycle Manager) - Core Infrastructure
# Version: v1.5.1
# Source: operator-controller GitRepository with ArtifactGenerator
#
# Note: ClusterCatalog resources are in separate base/olmv1-catalog/
# to ensure they deploy AFTER OLM CRDs are installed

resources:
  - gitrepository-olmv1.yaml
  - artifactgenerator-olmv1.yaml
