---
# ServiceAccount for flux-operator installation via OLM
# Grants permissions for OLM to install and manage flux-operator
apiVersion: v1
kind: ServiceAccount
metadata:
  name: flux-operator-installer
  namespace: flux-system
  labels:
    app.kubernetes.io/name: flux-operator
    app.kubernetes.io/component: installer
