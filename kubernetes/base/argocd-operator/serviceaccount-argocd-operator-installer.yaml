---
# ServiceAccount for argocd-operator installation via OLM
# Grants permissions for OLM to install and manage argocd-operator
apiVersion: v1
kind: ServiceAccount
metadata:
  name: argocd-operator-installer
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-operator
    app.kubernetes.io/component: installer
