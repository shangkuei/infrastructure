# code-server Environment Variables - shangkuei-xyz-unraid (LinuxServer.io)
# Copy to .enc.env and encrypt with: make encrypt

# User/Group ID (match Unraid nobody:users for file permissions)
PUID=99
PGID=100
UMASK=022

# Timezone
TZ=Asia/Taipei

# Authentication
# Plain text password (for web UI and sudo)
PASSWORD=7h4xjC526FTi3LjzLMQk
SUDO_PASSWORD=7h4xjC526FTi3LjzLMQk

# Or use hashed passwords (preferred for security):
# HASHED_PASSWORD=$argon2i$v=19$m=4096,t=3,p=1$...
# SUDO_PASSWORD_HASH=$argon2i$v=19$m=4096,t=3,p=1$...

# Proxy domain (for reverse proxy setups)
PROXY_DOMAIN=code.shangkuei.xyz

# Default workspace path inside container
DEFAULT_WORKSPACE=/workspace

# External port (only used if not behind reverse proxy)
CODE_SERVER_PORT=8443

# Workspace directory (on host - Unraid paths)
CODE_SERVER_WORKSPACE=/mnt/user/develop

# Additional appdata mount (optional, set in override)
CODE_SERVER_APPDATA=/mnt/user/appdata

# Config directory (on host - for volume binding in override)
CODE_SERVER_CONFIG=/mnt/user/appdata/code-server

# Docker mods for additional functionality
# Pipe-separated list: https://mods.linuxserver.io/?mod=code-server
DOCKER_MODS=linuxserver/mods:universal-git|linuxserver/mods:universal-package-install|linuxserver/mods:code-server-golang|linuxserver/mods:code-server-python3|linuxserver/mods:code-server-rust|linuxserver/mods:code-server-flutter|linuxserver/mods:code-server-nodejs|linuxserver/mods:code-server-nvm|linuxserver/mods:code-server-pnpm|linuxserver/mods:code-server-shellcheck|linuxserver/mods:code-server-terraform|linuxserver/mods:code-server-zsh

# Additional packages to install (space-separated)
INSTALL_PACKAGES=gcc

# Tailscale authentication (if using Tailscale overlay)
# Generate at: https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=tskey-auth-xxxxx
TS_EXTRA_ARGS=--hostname=code
