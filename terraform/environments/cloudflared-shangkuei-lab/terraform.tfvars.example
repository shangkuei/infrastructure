# Cloudflared shangkuei-lab Environment Variables
# Copy this file to terraform.tfvars and fill in your values
# DO NOT commit terraform.tfvars as it contains sensitive information

# Cloudflare Configuration
cloudflare_api_token  = "your-cloudflare-api-token-here"
cloudflare_account_id = "your-cloudflare-account-id"
cloudflare_zone_id    = "your-zone-id-for-shangkuei-xyz"

# Note: Actual values should be encrypted with SOPS
# Run: sops -e terraform.tfvars > terraform.enc.tfvars
# Run: sops -d terraform.enc.tfvars > terraform.tfvars (for local use)
